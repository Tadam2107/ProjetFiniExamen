 <!--== 4. Navigation ==-->
        <nav id="template-navbar" class="navbar navbar-default custom-navbar-default navbar-fixed-top ">
            <div class="container">
                <!-- Brand and toggle get grouped for better mobile display -->
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#Food-fair-toggle">
                        <span class="sr-only">Barre de navigation</span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    <a class="navbar-brand" href="#">
                        <img id="#" src="{{asset('images/')}}" class="logo img-responsive" alt="Magnifique logo">
                    </a>
                </div>

                <!-- Collect the nav links, forms, and other content for toggling -->
                <div class="collapse navbar-collapse" id="Food-fair-toggle">
                    <ul class="nav navbar-nav navbar-right">
                        <li><a href="{{path('accueil')}}">Accueil</a></li>
                        <li><a href="{{path('recettes')}}">Voir les recettes</a></li>
                        <li><a href="{{path('contact')}}">Contact</a></li>
                        {% if not app.user %}
                        <li><a href="{{path('app_register')}}">Inscription</a></li>
                        <li><a href="{{path('app_login')}}">Connexion</a></li>
                        {% else %}
                        <li><a href="{{path('recettes_user')}}">Vos recettes</a></li>
                        <li><a href="{{path('app_logout')}}">Deconnexion</a></li>
                        {% endif %}

                        {% if is_granted('ROLE_ADMIN') %}
                        <li><a href="{{path('admin_categories')}}">Gestion categories</a></li>
                        <li><a href="{{path('admin_recettes')}}">Gestion recettes</a></li>

                    
                </div><!-- /.navbar-collapse -->
                    {% endif %}
                </ul>
            </div><!-- /.row -->
        </nav>

      
